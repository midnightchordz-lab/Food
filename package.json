{
  "name": "moodfood-app",
  "version": "1.0.0",
  "description": "MOOD FOOD - When Feelings Need Feeding",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "cap:sync": "cd frontend && npx cap sync",
    "cap:build:ios": "cd frontend && npm install && npm run build && npx cap sync ios",
    "cap:build:android": "cd frontend && npm install && npm run build && npx cap sync android"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/user/moodfood-app.git"
  },
  "keywords": ["meal-planning", "mood-food", "capacitor", "react"],
  "author": "MOOD FOOD Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
